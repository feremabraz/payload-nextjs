# Environment Configuration for Payload CMS + Next.js

# Application
NODE_ENV=development
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Payload CMS Secret (required, minimum 32 characters)
PAYLOAD_SECRET=your-secret-key-here-at-least-32-characters-long

# Database Configuration
# For local development: postgresql://user:password@localhost:5432/database_name
# For Docker development: postgresql://postgres@postgres:5432/payload_db
POSTGRES_URL=postgresql://postgres@localhost:5432/payload_db

# Vercel Blob Storage for media uploads
# Get this token from your Vercel dashboard
BLOB_READ_WRITE_TOKEN=your-vercel-blob-token-here

# Optional: Force use of Vercel Postgres adapter even for localhost URLs
# FORCE_USE_VERCEL_POSTGRES=false

# Optional: Disable Next.js telemetry
# NEXT_TELEMETRY_DISABLED=1

# Optional: Skip migrations on build (useful for Vercel deployments)
# PAYLOAD_SKIP_MIGRATIONS=false

# Optional: Ignore data loss warnings during migrations (development only)
# PAYLOAD_IGNORE_MIGRATION_DATA_LOSS=false

# Email Provider
RESEND_API_KEY=your-resend-api-key-here
# Use Resend's default domain for testing (replace with your domain when available)
EMAIL_FROM=onboarding@resend.dev
